import { OnlyCharsDirective } from './only-chars.directive';

describe('OnlyCharsDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyCharsDirective();
    expect(directive).toBeTruthy();
  });
});
